PORT = 3000
DATABASE_URL = 'dbtype://user:password@host:port/db_name'
API_KEY = ####
JWT_SECRET = jwtSecretKey
NODE_MAIL = mail
NODE_MAIL_PASS = key provided by gmail

//Este solo es un archivo para darle un ejemplo al equipo de trabajo de lo que tiene que contener las variables de entorno ya que el archivo .env,
por defecto esta ignorado dentro de .gitignore